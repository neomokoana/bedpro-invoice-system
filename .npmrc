# Prevents the Prisma + pnpm symlink split where the generated client engine
# binary lands in a different .pnpm path than Node's module resolver follows.
# Forces a flat node_modules.
node-linker=hoisted
public-hoist-pattern[]=@prisma/*
public-hoist-pattern[]=prisma
public-hoist-pattern[]=@sparticuz/chromium
